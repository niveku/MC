/* Ejercio 2.1 */
/* Escrito por Kevin Henao */ 
/* Algo sencillo en C que debe recivir algo en pulgadas y presentar las conversiones. */

# include <stdio.h>

void main()
{
	const float inches_to_yards = 0.0277777778; /* Constante de cambio entre pulgadas y yardas. */
	const float inches_to_feet = 0.0833333333; /* Constante de cambio entre pulgadas y pies. */
	float yards; /* Declaración del float para la conversión a yardas. */
	float inches; /* Declaración del float para el valor ingresado en pulgadas. */
	float feet; /* Declaración del float para la conversión a pies. */
	printf("CONVERTING UNITS \nEnter the distance in inches to convert: \n"); /* Presentación de la acción interactiva. */
	scanf("%f", &inches); /* Lee y guarda el valor ingresado para la distancia es pulgadas. */
	yards = inches* inches_to_yards; /* Conversión de pulgadas a yardas. */
	feet = inches* inches_to_feet; /* Conversión de pulgadas a pies. */
	printf("Inches: %f \nYards: %f \nFeet: %f  \n", inches, yards, feet); /* Presentación de los resultados. */
	
}
